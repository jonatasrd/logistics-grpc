micronaut:
  application:
    name: logisticsGrpc

grpc:
  client:
    plaintext: true
    max-retry-attempts: 10
  channels:
    quote:
      address: 'localhost:50051'
      plaintext: true
      max-retry-attempts: 10
